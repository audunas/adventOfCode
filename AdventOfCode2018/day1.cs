using System;
using System.Collections.Generic;
using System.Text;

namespace AdventOfCode2018
{
    //static void Main(string[] args)
    //{
    //    var lines = File.ReadLines(@"..\..\..\input\day1.txt").ToList();
    //    HashSet<int> foundFrequencies = new HashSet<int>();

    //    var currentNumber = 0;
    //    foundFrequencies.Add(0);

    //    var frequenciesFoundTwice = false;
    //    int foundFrequencyResult = int.MaxValue;

    //    while (!frequenciesFoundTwice)
    //    {
    //        foreach (var line in lines)
    //        {
    //            currentNumber += int.Parse(line);
    //            if (foundFrequencies.Contains(currentNumber))
    //            {
    //                frequenciesFoundTwice = true;
    //                foundFrequencyResult = currentNumber;
    //                break;
    //            }
    //            foundFrequencies.Add(currentNumber);
    //        }
    //    }

    //    Console.WriteLine(foundFrequencyResult);
    //    Console.ReadLine();
    //}
}
